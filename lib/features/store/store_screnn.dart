import 'package:flutter/material.dart';

class StoreScrenn extends StatelessWidget {
  const StoreScrenn({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('store'),);
  }
}